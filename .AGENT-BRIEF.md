# dsh-muv-engine 待办规格（2026-09-20 对抗性 review 分派）

来源：老法师（对抗性 review）+交叉复核（独立复现）。**两条都已独立复现，不是推测。**

## 当前状态（先读，别重复劳动）
- `lib/client.js` 工作区有未提交改动（+981 行），其中包含 **ERA 事件应答桥** 与兼容垫片。
  **语法 OK，门禁全部持平基线**：`test-client-render` 174/2、`test-status-cascade` 84/0、
  `test-regex-engine` 21/0、`verify-fence-hijack-main` 37/0、`verify-visual` 64/10、
  `verify-frame-size` 32/0（单独跑）/ 30/2（批跑，已知抖动）。
- **上一轮代理在验证中途被服务器重启打断**，留下两个未经验收的新门禁：
  `verify-card-compat.mjs`（34KB）、`verify-era-bridge.mjs`（28KB）。
  ⚠️ **这两个门禁是否真能跑、读数是否可信，尚未确认** —— 请先跑通它们并报告真实数字。
- **`lib/client.js` 由一个人独占**。测试文件由另一个人负责。别互相踩。

## 已知基线（不许放宽断言来"变绿"）
★ **2026-09-20 更新**：`verify-visual` 的 **64/10 已查明是测试侧自伤，不是产品缺陷** ——
已修成 **77 通过 / 0 失败（exit 0）**（74 条基线 + 3 条新增）。**新基线就是 77/0**，
若你看到它变红，那是**真回归**，别再当成"预期红"。
其余：`test-client-render` 的 2 条预存失败；`verify-decorate-dom` 的 `G_gamecard` 是**预期红**。

★ `verify-era-bridge.mjs` **可信**（已独立跑通）：after 19/0，`nobridge` 臂 13/6、
`before(324b751)` 臂 6/13 —— **两条对照臂都在主断言上红**，且数据源是从真 3080 接口取回的。
★ `verify-card-compat.mjs` **至今从未跑通**：崩在 `spawnFrame` 的
「新 iframe 没有被 autoAttach 抓到（OOPIF 会话缺失）」，根因已定位（KEEP 名字正则漏了
`cardHtmlIFrame`），正在修 —— **别把它的读数当证据**。

---

## P0-1 ★ `window.__muvHFit` 是**只增不减的棘轮**，没有复位、没有测试
`lib/client.js:926-931`：
```js
'var bOver=body.scrollHeight>body.clientHeight+1;'
'var fit=window.__muvHFit||0;'
'if(bOver||dOver){var need=Math.max(body.scrollHeight,de?de.scrollHeight:0);'
'if(need>fit){window.__muvHFit=need;fit=need}}'
if(fit>maxB)maxB=fit;
```
一旦记下某值，它**永久**成为帧高下界，没有任何降回路径。两个真实反例：
- **内容缩小 ⇒ 永久死白**：帧高 900、内容 1200 → 棘轮记 1200 → 父页设 1200 → 用户折叠了
  卡里面板/图片变成 300 → `bOver` 为假、`fit` 仍是 1200 ⇒ 卡片底下常年 900px 空白。
- **一次坏读数锁死高度（更危险）**：注释自己承认 `extent()` 有漏算。若首帧（`load`/700ms/
  1600ms 时远程插图还没 decode、`getBoundingClientRect()` 高度 0 或占位高）记下一个**偏小**的
  `body.scrollHeight`，该偏小值就锁死帧高，而 reset 是 `overflow:hidden!important`
  ⇒ **内容被永久裁掉**。

这与交接文档「`h = muvContentExtent()`，**不要 `max` 任何 `scrollHeight`**」硬冲突。

**要做**：
1. 棘轮**必须能降**：连续 N 次（≥3）观测到「不溢出」就把 `__muvHFit` 归零重学；**带滞回**，别抖。
2. `document.readyState !== 'complete'` 或首帧（`load` 之前）**不记账**。
3. ★ **加收缩方向的门禁**：`verify-frame-height.mjs` 现在只验「600/900/1500 收敛到同一值」，
   **收缩方向零覆盖** —— 这正是它藏在门禁之外的原因。加「内容缩小后报数必须回落」。
4. 最小复现（不需要真浏览器）：把 `muvFrameBootstrap` 逐字提取，假 `document` 让 `extent()`
   返回 100、`body.scrollHeight=1200`，跑 `m()` → 棘轮 1200；再让内容只剩 50 且
   `scrollHeight=400`，跑 `m()` → 现在仍报 1200（这就是 bug）。

## P0-2 ★ 三处幂等守卫都是「查子串」，同一形状、同一静默失效模式
- `lib/client.js:1262` `if (s.indexOf('__muvReset') !== -1) return s`
- `lib/client.js:1560` `if (s.indexOf('__muvCompat') !== -1) return s`
- （`__muvH` 那条已修成完整标记 `window.__muvH=1`，但**类未消**，见下）

失效模式：卡的原始 HTML 里若出现该串（模型跑题、作者抄别家 shim、卡里内嵌文档）
⇒ 那段脚本**整段被跳过** ⇒ 卡的 `localStorage` / `SillyTavern.getContext()` 全塌，
**静默无日志**。`__muvCompat` 跑在**最内层、直接面对卡原文**，处境最危险。

★★ **残留类 + 指标陷阱**（交叉复核实测）：卡文档里写 `window.__muvH=1;` 时
- 引导脚本**仍会被顶掉**（该臂 `__muvHFit`=0、`__muvFrameHeight`=0，正常链是 2 / 1）
- 但此时数 `window.__muvH=1` 仍读到 **1**（那是卡自己的拷贝）
⇒ **"只注入一次"这类断言会因错误的原因通过**。

**要做**：
1. **根治**：幂等状态放**父页**（`cardHtmlIframe` 里同一个 `raw` 只组一次注入链、结果缓存进
   `Map`），子文档只执行、不自我判断。这一改直接消掉整个类。
2. 若保留子文档侧守卫，判据必须是**引导脚本专属 token**（`__muvHFit` / `postMessage` key），
   **不是** `window.__muvH=1`。
3. 加**对抗用例**：卡文档自带 `window.__muvH=1;`，断言引导脚本**仍然**注入。
4. 已确认改小守卫**不会**引入双重注入：注入链是
   `withFrameHeightBootstrap(withCardReset(withCardCompat(rewriteVhMinHeight(raw))))`，
   compat 在内层先跑、产物只有 `__muvHello`（不含 `window.__muvH=1`）。

## P0-3 ★ `verify-visual.mjs` 的 `stripBootstrap` **定位错了脚本**（测试侧）
`verify-visual.mjs:350` 附近用 **`indexOf('__muvH')`** 这个**子串**定位引导脚本。
但 `__muvH` 会**先命中兼容垫片**（垫片里有 `__muvKvSeed` 和 `post({__muvHello:1})`，
两者都**包含** `__muvH`），而 compat 在**内层、先跑**、位置更靠前
⇒ `stripBootstrap` 剥掉的是**垫片那段 `<script>`**，不是引导脚本 ⇒ `un === t.body` 必然不等。
**这解释了 `verify-visual` 那 64/10 里的"剥后比正文多 246 字符、首处差异恒在 `@42`"**
——很可能是**自伤**，不是浏览器抖动。
参考：`repro-frame-height.mjs:79` 与 `test-client-render.mjs:495` 用的是完整标记，不受影响。
**★ 已修完（2026-09-20，实测）—— 真实成因与 brief 原判"只对了一半"：**
- 机制成立：`indexOf('__muvH')` 命中 @4456（**垫片内部**；垫片 `<script>` 在 @288 / len 4197）
  ⇒ 剥掉的是**垫片整段**。
- **但改用完整标记 `window.__muvH=1` 并不能修好，反而更差**：子串定位差 **1910** → 完整标记差 **4443**。
- 真因：注入链是**三段**，不是一段。`withCardReset` 在第一个 `<head>` 后插 viewport meta +
  `<style data-muv-reset="__muvReset">`（≈242 字）⇒ **@42 正是那段 meta 的起点**
  （@42 = 第一个 `<head>` 的 `>` 之后），所以「首处差异恒在 @42」是**必然**，与浏览器抖动无关；
  随后 `withCardCompat` 插垫片（4197）；`withFrameHeightBootstrap` 在最后一个 `</body>` 前插引导（1664）。
  242+4197 = 4439 ≈ 4443（余数是 escAttr 往返）⇒ **只剥引导脚本，逐字相等永远不可能成立。**
- 修法：`scriptBlocks()` / `stripInjected()` 按**各段专属 token**（引导 `__muvHFit`、垫片 `__muvKvSeed`、
  reset `data-muv-reset="__muvReset"` + 紧随的 viewport meta）剥掉**整条链**，再要求逐字相等；
  失败详情要报「剥掉的段 + 差多少 + 首处不同」。
- 注入计数判据：数「含 `__muvHFit` 的 `<script>` **段数** == 1」，**不是**数 `window.__muvH=1`
  （卡自己也能写那个串 ⇒ 断言会**因错误的原因通过**）。已加 §2b 对抗用例（卡自带 `window.__muvH=1;`）。

## ★ 已裁定：`neither` 分支**有意偏离 ST**（别再"修回去"）
`C:\dsh-muv-engine\lib\regex-engine.js` 的 placement 语义已对齐 ST,但**唯一一处有意偏离**:
- ST:`markdownOnly`/`promptOnly` **两个都没开**的脚本,在 `isMarkdown` 时**不再跑**
  （因为 ST 在更早的 raw/source 阶段已经跑过它,注释原话"neither … 无需再跑"）。
- **本引擎只有渲染一个阶段** ⇒ 若照 ST 跳过,这类脚本**永远不会生效** —— 那比 ST 的用户可见结果**更差**。
- 故实现为 **`neither` → 两侧都跑**,并在代码注释与门禁标题里标注「有意偏离 ST」。
- **裁定理由**:① 保住既有卡作者的行为（`test-regex-engine.mjs` 的 21/0 基线就编码了这一点）;
  ② 与 `extractStatusBarHtml` 的改法一致（**只放宽、不收紧**的 superset）;③ 没有任何证据表明用户要严格 ST。
- **后果**:若有人改回严格 ST,`test-regex-engine.mjs` 会红 20/1
  （`line 94-95` 期望"无标志脚本在显示侧生效"）。**红是预期,不要靠改产品代码去消它**;
  真要改就改断言(`line 94` 的 `sample` 补 `markdownOnly: true`),并且必须**先问过用户**。

## 已完成（2026-09-20）
- **`regex-engine.js` placement + 深度门**:`placementAllows` / `depthAllows` / `matchesMode`(已 export) /
  `scriptRunsOnDisplay` / `applyCardScripts({depth})`。门禁 `verify-regex-modes.mjs` **44/0**。
  depth 口径 = ST:`0` 是**正在渲染的那一条**;`minDepth:null` ≠ `0`（单列断言钉住）。
  真卡真文本 before/after:`applied` 1→2、裸 `<VariableInsert>` 1→0、裸 JSON 键 4→0。
  ★ `mode` 由 `lib/index.js` 的 `pickPlacement()` 决定(只认 `'prompt'`/`'all'`,其余一律 `'display'`),
  **两处调用点都是显示侧**(`isMarkdown=true, isPrompt=false`,与 ST `script.js:1809` 同一对)。

---

## P1-3 ★ 服务端堵了，但**上游客户端还在猜、而且被标成 `explicit`**
`lib/client.js:269-275`：
```js
if (sid) params.push('sessionId=' + ...)
else { var pid = currentPresetId(); if (pid) params.push('presetId=' + ...) }   // ← 旧值，无声明
```
服务端 `!preferPreset` 分支会对 presetId 调 `fromExplicit()` ⇒ **收下并标
`presetSource='explicit'`**（听起来像"用户明确指定"，其实是猜）⇒ 串台向量没堵死，
只是从服务端的 `active` 搬到了客户端的 `explicit`，标签还更自信。
**要做**：和酒馆侧对齐 —— **认不出会话就什么都不带**，不要拿 `currentPresetId()` 当 `presetId` 送。

## P1-4 ★ 客户端 bundle 里的 `presetSource` 与那两条契约
- `C:\dsh-muv-table\CHANGELOG.md` 的行为表**已同步**（`preferActive=1` 才可达 `active`）。
- `C:\dsh-muv-engine\diag.mjs` 的无参诊断语义**已改**（无参恒为稳定默认，不代表定位正确）。
- 待办：**没有任何客户端消费 `presetSource`**（`HANDOFF.md` 已记为缺口）。面板应在
  `default`/`session-default` 时提示「本会话未绑定卡，正在用酒馆默认预设」+ 一键跳绑卡。

## P2（都已被点名为真问题）
- **`muvKv` 无界增长**（`:1412`/`:1453`/`:1672`）：400 条 / 2MB 是**每键**上限，
  **键的总数没有上限**，而键 = 内容散列 + 长度，**内容一变就是新键**（编辑消息、流式重渲染）。
  `muvKv` 是**页面级生命周期**，无 LRU、无 delete、无清理钩子。按 30 个历史键 × 最坏 2MB = 60MB 常驻。
  → 加 LRU（16~32 键或 8MB 总量）；会话/卡切换时清不可达键；键改成**卡身份**（文件名/预设 id）
  而不是内容散列（内容散列本来就不是身份，还让"同一张卡改一个字就丢光 CG 缓存"）。
- **`muvChatLog` 是全局的、跨会话回送**（`:1433`/`:1598`/`:1569`）：上限没问题
  （80/40000/600000），**隔离**有问题 —— 不区分会话、不区分卡，而 `muvReplyToFrame` 把
  **全量 80 条**发给**每一个**卡 iframe ⇒ A 会话的消息会出现在 B 会话卡的 `getContext().chat` 里，
  卡的 `cgScanChat` 扫 `<img>名</img>` ⇒ **在 B 会话能解锁 A 会话的 CG**，
  且 A 会话正文（含世界书文本）被送进 B 会话 iframe。
  → 用 `getCurrentSessionId()` 当栅栏，会话 id 一变就 `muvChatLog.length = 0`（顺手把 `muvKv` 清理挂上）。
  → `muvPushChatLog` 的"替换而非追加"判据只覆盖前缀延伸，覆盖不到「消息被编辑成前后无关文本」
    ⇒ 会变成新增一条、把 80 条额度吃两份。
- **`__muvHello` 无速率限制**（`:1665`）：沙箱卡可以
  `setInterval(()=>parent.postMessage({__muvHello:1},'*'),0)`，每次 hello 都跑一遍
  `muvCompatSeedMap`（深遍历最多 400 条 KV 重建对象）+ `muvChatList`（80 条/600KB 截断）
  + 两次 `postMessage`（结构化克隆）⇒ **父页主线程可被放大打满**。
  → 按帧记 `lastReplyAt`，同帧 200ms 内只回一次，且仅在快照真变过时回。
- **idempotency 标记只写不查监听器身份**（`:1003` `__muvFrameHListener`、`:1545`
  `__muvCardCompatListener`）：标记挂 window、值只记 `true`；监听器闭包持有 `muvKv`/`muvChatLog`。
  插件重载时新闭包有新的空 `muvKv`，但旧监听器仍在页面上且被标记挡着 ⇒ 老 iframe 的消息写进
  **旧的孤儿状态**、新 iframe 拿新状态 ⇒ CG 解锁在重载后丢失，旧闭包永不回收。
  → 存**引用**而不是 `true`，不一致时 `removeEventListener` 旧的再绑新的。

## 次要（可维护性）
- `rewriteVhMinHeight(raw, hostH)` **实参 2 个、形参 1 个**（定义 `:1085`、调用 `:1721`）：
  功能无害（函数内自取 `window.innerHeight`），但遮蔽真实契约。要么补形参、要么删实参。
- 注释过期：`:921-925` 写着「reset 给 html/body 加了 `overflow-y:auto`（照 ST）」——
  **现在 reset 是 `overflow:hidden!important`**（`:1233`，有门禁钉着不许回退）。
  `overflow:hidden` 下 `scrollHeight` 仍报告溢出距离，所以**行为没坏**，但**理由必须重写**。

## 仓库卫生
`C:\dsh-muv-engine` 根目录留了一批临时产物：`_st-baseline/`、`_st-verify/`、`_vid-invest/`、
大量 `.tmp-*.mjs`，其中 `.tmp-old-client.js` / `.tmp-old-client-324b751.js` /
`.tmp-era-old-client-324b751.js` 是**整份 client.js 副本**，且 `_st-verify/src-after.js` 里那份是
**旧版实现** —— 留着迟早被人当成"最新源码"读。**先别急着删**（还可能在用），但要在报告里点名。

## 铁律
- **不要 commit / push / publish**；不要 `git add -A`；只显式加路径。
- **不要重启 3080 上的 DSH**（用户在用）。
- 卡沙箱必须仍是 `MUV_CARD_SANDBOX = 'allow-scripts'`，**绝不加 `allow-same-origin`**。
- 本项目测试常把函数**逐字提取**出来 `new Function` 执行 ⇒ **函数体里不要出现代码位置的除号 `/`**
  （用 `* 0.01` 之类代替），否则提取器词法扫描会崩、报 `Invalid regular expression: missing /`，
  而错误会指向**别的函数**，极难反查（刚踩过）。
- Windows：只有 PowerShell 5.1（**没有 `pwsh`**）；`>` 会写成 UTF-16（用 Node `fs.writeFileSync`）；
  **别用 `Get-Content` 读 UTF-8**（GBK 乱码）；ESM 绝对路径要 `file:///C:/...`。
